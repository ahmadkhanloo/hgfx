# HGFX v1.0.0 Final Promotion Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Promote the independently reviewed HGFX release candidate to final `1.0.0` without changing frozen scientific criteria or historical evidence.

**Architecture:** Treat final promotion as a release-only change. First update package/citation metadata and live release-status documentation on an isolated release branch, then validate the exact revision through existing CI, merge that validated revision to `main`, validate `main`, and only then create/tag the final release provenance.

**Tech Stack:** Python packaging (`pyproject.toml`/Hatchling), CITATION.cff, GitHub Actions, existing HGFX release-check scripts.

**Spec:** `docs/planning/V1_RELEASE_GATE.md`

**Completion status (2026-09-16): COMPLETE.** `v1.0.0` is published from validated source `4dd8fbd8239d05f2c7932a9a9b3b7795f0a9ab27`; GitHub Release ID `389966452`.

## Global Constraints

- Frozen MATLAB oracle remains HGF Toolbox 8.2.0 @ `2437f4dc241541072722a2695ddeca7b44d83dd3`.
- Do not change thresholds, seeds, datasets, grids, model family, optimizer, historical failures, or physical-GPU acceptance criteria.
- Preserve the independent review report and remediation evidence.
- Final package and citation version must be exactly `1.0.0`.
- Final release status requires fresh CI evidence on the exact promoted revision.

---

### Task 1: Promote final version metadata

**Files:**
- Modify: `pyproject.toml`
- Modify: `CITATION.cff`
- Modify: `README.md`
- Modify: `docs/planning/V1_RELEASE_GATE.md`
- Modify: `docs/planning/V1_TODO.md`

- [x] **Step 1:** Change package version from `1.0.0rc1` to `1.0.0` in `pyproject.toml`.
- [x] **Step 2:** Change citation version from `1.0.0rc1` to `1.0.0` in `CITATION.cff`.
- [x] **Step 3:** Synchronize README and live release documents with the already-passed PR #29/main integration evidence.
- [x] **Step 4:** Commit the promotion-only change on `release/v1.0.0`.

### Task 2: Validate the exact promotion revision

**Files:**
- Test: existing GitHub Actions release/regression workflows and release-check scripts.

- [x] **Step 1:** Open a PR from `release/v1.0.0` to `main`.
- [x] **Step 2:** Verify all required PR-head checks that run for the promotion revision.
- [x] **Step 3:** Inspect any failed job logs; do not merge on failure.

Evidence: S10 `35089882319`, M19/M20 preflight `35089882608`, D10/D11 `35089882668`, Regression `35089882392` (Ubuntu + Windows).

### Task 3: Integrate and verify final main revision

**Files:**
- No new implementation files unless CI exposes a release-blocking defect.

- [x] **Step 1:** Merge the validated promotion PR to `main`.
- [x] **Step 2:** Verify required main/push checks on the merge commit.
- [x] **Step 3:** Record exact merge commit and workflow run IDs in release provenance/status docs.

Evidence: PR #30 merged at `4dd8fbd8239d05f2c7932a9a9b3b7795f0a9ab27`; main Regression `35090329868` PASS on Ubuntu + Windows.

### Task 4: Final tag/release provenance

**Files:**
- Modify/create release provenance documentation only if needed to record the exact final release target.

- [x] **Step 1:** Create final `v1.0.0` tag on the fully validated release revision.
- [x] **Step 2:** Create the GitHub Release for `v1.0.0` without altering scientific evidence.
- [x] **Step 3:** Record exact commit/tag/release provenance and mark the final release gate complete.

Verification: `refs/tags/v1.0.0` resolves directly to `4dd8fbd8239d05f2c7932a9a9b3b7795f0a9ab27`; Release ID `389966452`, published at https://github.com/ahmadkhanloo/hgfx/releases/tag/v1.0.0, `draft=false`, `prerelease=false`.

## Completion

All final-promotion tasks are complete. The v1.0.0 release evidence is frozen; future work requires a new post-v1 scope rather than reopening this plan.
