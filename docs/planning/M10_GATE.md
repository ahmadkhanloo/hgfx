# M10 — Hessian/LME Parity Gate

Status: **PASS**

## Frozen reference

- HGF Toolbox: `8.2.0`
- Commit: `2437f4dc241541072722a2695ddeca7b44d83dd3`
- Fit orchestration: frozen `fitModel.m`
- Hessian: frozen `riddershessian.m`
- Passing workflow run: `34162675825`
- Numerical mode: CPU float64

## Gate definition

M10 passes when post-MAP fit statistics and LME-based restart selection match the frozen MATLAB reference while preserving the M8 objective and M9 optimizer boundaries.

## Default fit statistics validated

For `hgf_binary + unitsq_sgm`:

- [x] MAP vector remains M9-compatible
- [x] Ridders Hessian at the MAP
- [x] `init_h=1`
- [x] `min_steps=10`
- [x] Hessian positive-definite decision
- [x] `Sigma = inv(H)` on the valid numerical-Hessian branch
- [x] `nearest_psd(H)`
- [x] `nearest_psd(Sigma)`
- [x] posterior `Corr`
- [x] `negLl`
- [x] `negLj`
- [x] Laplace `LME`
- [x] `decompLME.logjoint`
- [x] `decompLME.postpredcorr`
- [x] `decompLME.freepars`
- [x] accuracy
- [x] complexity
- [x] AIC
- [x] BIC

## Fallback branch validated

An explicit non-positive numerical-Hessian oracle validates the frozen fallback:

```text
H = inv(T)
Sigma = T
H = nearest_psd(H)
Sigma = nearest_psd(Sigma)
Corr = Cov2Corr(Sigma)
LME = -valMin + 0.5*log(1/det(H)) + d/2*log(2*pi)
```

This branch is tested directly against MATLAB formulas rather than assumed from the normal fit.

## Multi-start validation

The frozen implementation chooses a random-restart result only when:

```text
candidate.LME > current_best.LME
```

M10 validates this selection by:

1. setting a fixed MATLAB seed;
2. exporting the exact transformed-space free startpoints generated by MATLAB;
3. running those same startpoints through the Python compatibility fitter;
4. computing full Hessian/LME statistics for every candidate;
5. confirming that Python selects the same final fit as MATLAB `fitModel`.

This is **restart-selection parity**. It deliberately does not claim byte-identical MATLAB/NumPy normal RNG streams.

## Evidence

Workflow run `34162675825`:

- `python-fit-statistics-tests`: PASS
- `matlab-python-hessian-lme`: PASS

The initial M10 implementation passed the MATLAB oracle without a tolerance adjustment or scientific-code correction.

## Architecture boundary

M10 completes the CPU compatibility fitting/statistics chain:

```text
parameters/config
→ forward model
→ observation likelihood
→ objective
→ Ridders+BFGS MAP
→ Ridders Hessian
→ Sigma/Corr
→ Laplace LME
→ AIC/BIC
→ LME-based restart selection
```

## Next milestone

`M11 — Simulation Parity`
