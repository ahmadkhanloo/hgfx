# M18 completion plan — MATLAB-equivalent v1.0

Last synchronized: 2026-09-15
Status: **IN PROGRESS — S9/S10/M19 CLOSED; ONLY M20 FINALIZATION REMAINS**
Branch: `migration/m18-workflow-closure`
Frozen MATLAB reference: `2437f4dc241541072722a2695ddeca7b44d83dd3`

## Objective

Deliver HGFX v1.0 as a functional/scientific Python replacement for MATLAB HGF Toolbox 8.2.0, with no MATLAB runtime dependency for users. Compatibility is judged under frozen equivalence/reference-limitation policies. Historical and prospective failures remain immutable.

## Current baseline

| Item | Status | Evidence |
|---|---|---|
| Historical M18 scientific experiment | FAIL, preserved | immutable historical gate |
| M0-M17 | PASS in documented scopes | milestone evidence |
| D02 direct fit | FAIL, preserved | historical/direct evidence |
| D02 exact official release disposition | `REFERENCE_LIMITATION_MATCH` | exact paired evidence |
| D08 prospective Level-2 holdout | FAIL, preserved | repaired-product evidence |
| D08 exact failed-seed disposition | `REFERENCE_LIMITATION_MATCH` | exact paired evidence |
| D09/D10-D11/D12 | PASS | recorded workflow runs |
| S7 paired parameter recovery | `REFERENCE_LIMITATION_MATCH` | frozen paired grid |
| S7 paired model selection | `PASS_PAIRED_MODEL_SELECTION` | 36/36 BIC winners match |
| S8 evidence-backed repair | DONE | `0239f52f772825e0a4fc74cdf3559cafa18a603e` |
| S9 CPU/backend | `PASS_CPU_BACKEND_EQUIVALENCE` | run `34901924475` |
| S9 physical GPU | `PASS_PHYSICAL_GPU_APPLICABILITY` | 2x Tesla T4; source `07b45a569e04e8e71244c5310dd2cc53dbb2b0ec`; raw SHA-256 `6cd35c82be1e542830c06f6b7b7e444fda0ff4fe93773f080e6c13725212dfdf` |
| S10 release readiness | PASS | run `34934079865` |
| M19 evidence freeze | `PASS_FROZEN` | run `34966661492`; manifest commit `b71301b978e07cb0fa5ac2ad14cc92235fadc5ee` |
| M20 v1 candidate | READY TO FINALIZE | candidate metadata `1.0.0rc1`; final checker pending |

## S9 closure

S9 CPU/backend and physical-GPU applicability are closed. The accepted physical run satisfies the unchanged numerical gate with real GPU residency, all four required CPU-vs-GPU cases PASS, and maximum final-objective gap `1.4210854715202004e-14` below frozen `1e-7`.

The run used a hosted/shared Kaggle environment with 2x Tesla T4. This validates numerical backend applicability, not uncontended performance, scaling, or H100-specific performance. The captured `?? gpu_validation_results/` status is generated by the wrapper itself because it creates the output directory before provenance capture.

## M19 closure

M19 finalize workflow run `34966661492` completed successfully. It produced a committed machine-readable manifest with `status=FROZEN`, `failures=[]`, physical GPU PASS, and explicit integrity records that historical failures, thresholds, seeds, datasets and grids were not rewritten. The manifest commit is `b71301b978e07cb0fa5ac2ad14cc92235fadc5ee`.

Candidate metadata was subsequently promoted from `1.0.0.dev0` to `1.0.0rc1` in both package and citation metadata. Because those files are part of the M19 hashed release package, the final M20 sequence refreshes the M19 manifest hashes before executing the M20 checker. This is metadata synchronization only; no scientific/numerical evidence is changed.

## Anti-endless-patching rule

Do not chase floating-point micro-differences merely because they exist. Repair only when frozen evidence links a difference to a required semantic mismatch. Never widen tolerances or change seed/start/dataset/grid/model/optimizer after results to obtain PASS.

## Final M20 sequence

1. Refresh `reference/validation/v1_release/evidence_manifest.json` using `scripts/build_v1_evidence_manifest.py --mode finalize` so hashes cover the synchronized `1.0.0rc1` release files.
2. Run `scripts/check_m20_candidate.py --mode finalize` against that refreshed `FROZEN` manifest.
3. Declare M20 PASS only if the checker returns `PASS_M20_CANDIDATE` with zero failures.
4. After M20 PASS, synchronize release documents and only then make PR #26 ready/merge/tag according to repository policy.

## Next action

Execute the refreshed M19 manifest and M20 final checker. No further scientific retuning or GPU rerun is currently justified by the frozen gates.

## Maintenance validation — 2026-09-15

CI/docs revision `60472362d05412a35b046b197a32daacc05ebbef` passed local
CPU regression (167 passed, four physical-GPU skips), candidate clean-wheel
build/install/API import/quickstart, and the static reference/release checks.
M19 refresh and the M20 final checker passed locally with no failures.
Evidence: `reference/validation/ci_maintenance_20260915/`.

Fresh GitHub regression `34971334728` and S10 `34971334355` were queued
at inspection. M20/release closure remains IN PROGRESS pending fresh CI;
no merge/tag or new physical-GPU validation is claimed. Classic branch
protection could not be read (403); do not bypass required checks.
